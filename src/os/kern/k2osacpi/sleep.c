#include "k2osacpi.h"

ACPI_STATUS
AcpiOsEnterSleep(
    UINT8                   SleepState,
    UINT32                  RegaValue,
    UINT32                  RegbValue)
{
    K2_ASSERT(0);
    return ACPI_FAILURE(1);
}

